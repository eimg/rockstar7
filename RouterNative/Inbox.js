import React from 'react';
import { Container, Text, Body } from 'native-base';

const Inbox = props => {
	return (
		<Container>
			<Body><Text>Inbox</Text></Body>
		</Container>
	);
}

export default Inbox;