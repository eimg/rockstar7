import React from 'react';
import { Container, Text, Body } from 'native-base';

const Compose = props => {
	return (
		<Container>
			<Body><Text>Compose</Text></Body>
		</Container>
	);
}

export default Compose;
